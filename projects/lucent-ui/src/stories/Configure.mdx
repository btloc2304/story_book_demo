import { Meta } from "@storybook/blocks";

<Meta title="Lucent UI/Giới thiệu (Introduction)" />

<div className="sb-container">
  <div className='sb-section-title'>
    # 🚀 Chào mừng đến với Lucent UI
    
    Bộ thư viện Giao diện Người dùng (UI Components) chuẩn mực dành riêng cho hệ sinh thái Angular, thiết kế theo ngôn ngữ hiện đại, nhanh nhẹn và sắc nét.
  </div>

  <div className="sb-section">
    <div className="sb-features-grid">
      <div className="sb-grid-item">
        <h4 className="sb-section-item-heading">🎨 Tương thích Dark Mode & Light Mode</h4>
        <p className="sb-section-item-paragraph">Hỗ trợ trọn vẹn chủ đề màu tối và màu sáng từ tận lõi SCSS với các hằng số màu thiết kế khoa học.</p>
      </div>
      <div className="sb-grid-item">
        <h4 className="sb-section-item-heading">♿️ Accessibility (a11y) Compliant</h4>
        <p className="sb-section-item-paragraph">Mỗi Component đều được thiết kế vượt qua các bài kiểm thử độ tương phản màu và trải nghiệm đọc của W3C.</p>
      </div>
      <div className="sb-grid-item">
        <h4 className="sb-section-item-heading">🤖 Visual Regression Tested</h4>
        <p className="sb-section-item-paragraph">Hệ thống Chromatic tự động chụp lại ảnh màn hình pixel-by-pixel để đảm bảo layout không bị vỡ sau mỗi đợt update.</p>
      </div>
    </div>
  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }
  .sb-section-title {
    margin-bottom: 32px;
  }
  .sb-section-title #-\\-chào-mừng-đến-với-lucent-ui {
    font-size: 32px;
    font-weight: 700;
    margin-bottom: 8px;
    color: #3186FF;
  }
  .sb-section-item-heading {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 8px;
  }
  .sb-features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
  .sb-grid-item {
    padding: 24px;
    border-radius: 8px;
    background: #f1f5f9;
  }
  `}
</style>
